name: fly_bug
description: A new Flutter project.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_lints: ^2.0.0
  firebase_auth: ^3.10.0
  cloud_firestore: ^3.4.9
  progress_dialog_null_safe: ^1.0.7
  firebase_core: ^1.23.0
  provider: ^6.0.3
  get_it: ^7.2.0
  shared_preferences: ^2.0.15
  fluttertoast: ^8.0.9
  toast: ^0.3.0
  simple_animations: ^5.0.0+2
  intl: ^0.17.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.10.0

flutter_icons:
  android: true
  ios: true
  image_path: "lib/images/1024.png"

flutter:
  uses-material-design: true

  assets:
    - lib/images/
